{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Launch",
			"type": "go",
			"request": "launch",
			"mode": "debug",
			"program": "${workspaceFolder}",
			"buildFlags": "-ldflags='-s -w'",
			"env": {
				"GOARCH": "amd64" // или "amd64" для 64-битной Windows
			}
		}
	]
}
